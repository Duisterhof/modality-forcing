# Read by HF Spaces (Spaces cannot read pyproject.toml) and by plain-pip users.
# Keep in sync with pyproject.toml: this file = [project.dependencies] + torch
# + accelerate. uv users should `uv sync --extra cuXXX` instead (see README).
#
# NOTE on PyTorch for pip users: install a build matching YOUR CUDA driver
# FIRST, or pip may grab a wheel for a newer CUDA than your driver supports and
# the GPU will be invisible (torch.cuda.is_available() == False). See README.
#   e.g. CUDA 12.x:  pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.5
einops>=0.8
jaxtyping>=0.2
transformers>=4.45
safetensors>=0.4
huggingface_hub>=0.24
accelerate>=1.0
numpy>=1.26,<3
scipy>=1.11
opencv-python-headless>=4.8
pillow>=10
matplotlib>=3.8
trimesh>=4.4
gradio>=6,<7            # the interactive demo (app.py)
spaces                  # HuggingFace ZeroGPU runtime (no-op off-Space; app.py imports it)
